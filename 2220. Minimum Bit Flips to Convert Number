class Solution {
    public int minBitFlips(int start, int goal) {
         int num = start ^ goal;
         int result = 0;
         while(num > 0){
            result += (num&1);
            num >>= 1;
         }
         return result;
    }
}
